<button type="button" {{#if lower}}style="margin-top: 25px;" {{/if}} data-id="{{id}}"
  class="btn btn-{{#unless btn-class}}default{{else}}{{btn-class}}{{/unless}} btn-flat">
  {{#if btn-icon}}
    <span class="{{btn-icon}}"></span>
  {{/if}}
  <span data-i18n="{{text-i18n}}"></span>
</button>